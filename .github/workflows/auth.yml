name: CI-Auth

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - "auth/**"
  pull_request:
    branches: [ master ]
    paths:
      - "auth/**"

jobs:
  build-auth:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL=${{ env.DATABASE_URL }}
      OAUTH_PRIVATE_KEY=${{ env.OAUTH_PRIVATE_KEY }}
      OAUTH_PUBLIC_KEY=${{ env.OAUTH_PUBLIC_KEY }}
      OAUTH_ENCRYPTION_KEY=${{ env.OAUTH_ENCRYPTION_KEY }}
      APP_SECRET=${{ env.APP_SECRET }}
    steps:
      - uses: actions/checkout@v2

      - name: 🐳 Start all the environment Mysql
        run: make start-mysql

      - name: 🐳 Start all the environment Auth
        run: |-
          cd auth
          make start

      - name: 🔦 Lint Auth
        run: |-
          cd auth
          make cs

      - name: ✅ Run the tests
        run: |-
          cd auth
          make test